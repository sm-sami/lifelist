# Lifelist — agent guide

The canonical, agent-agnostic guide lives in `AGENTS.md` (imported below so it loads in
every Claude Code session). Keeping one source avoids drift across AI tools.

@AGENTS.md
