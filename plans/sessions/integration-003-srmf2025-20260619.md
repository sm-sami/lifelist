# Handoff — integration/003

- Author: srmf2025@gmail.com   Branch: phase/integration-003   Started: 2026-06-19 14:37Z   State: done

## Summary

Implemented realtime item sync: Supabase publication/private broadcast RLS migration, mobile realtime hook, and tab-level mounting beside hydration.

## Progress log

- 14:37 — Confirmed backend broadcaster already sends private `user:<userId>` / `item.enriched` payloads.
- 14:37 — Added idempotent realtime migration for `public.items` publication and `realtime.messages` private topic policies.
- 14:37 — Added `useItemsRealtime()` hook with `setAuth`, private broadcast subscription, Postgres Changes subscription, runtime validation, Hono refetch for DB changes, and leak-safe cleanup.
- 14:37 — Mounted realtime sync in the protected tabs layout.
- 14:37 — Applied and verified realtime migration locally.

## Done

- `apps/backend/drizzle/0004_realtime_item_sync.sql` publishes `public.items` to `supabase_realtime` and secures private broadcast channels through `realtime.messages` RLS policies.
- `apps/mobile/lib/realtime/useItemsRealtime.ts` subscribes to private enrichment broadcasts and owner-filtered Postgres Changes.
- Broadcast payloads are validated as `ItemDto`; Postgres Changes validate only item ids and refetch through Hono so raw storage paths never enter the store.
- Fast unmount/token-change cleanup removes created channels and tears down any late-created channel.
- `apps/mobile/app/(protected)/(tabs)/_layout.tsx` now mounts both hydration and realtime sync.

## Left / next

- Production Supabase realtime migration was not applied because production mutation requires explicit approval.
- The integration phase set is otherwise complete.

## Gotchas

- The client filter `user_id=eq.<userId>` is convenience only; `items` RLS remains the security boundary.
- Private broadcast privacy depends on both `{ config: { private: true } }` in the client and `realtime.messages` policies in the migration.

## Files touched

- `apps/backend/drizzle/0004_realtime_item_sync.sql`
- `apps/backend/drizzle/meta/_journal.json`
- `apps/backend/db/migration.test.ts`
- `apps/mobile/lib/realtime/useItemsRealtime.ts`
- `apps/mobile/app/(protected)/(tabs)/_layout.tsx`

## Verification

- pnpm gate: PASS   tests: PASS
- Local migration: PASS
- Local realtime publication/policies verification: PASS
- Production migration: PENDING explicit approval
