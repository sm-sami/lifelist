# Handoff — integration/002

- Author: srmf2025@gmail.com   Branch: phase/integration-002   Started: 2026-06-19 14:34Z   State: done

## Summary

Implemented the private item-image pipeline: Supabase Storage bucket/RLS migration, mobile picker/upload flow, backend image route hardening, and item-detail photo replacement UI.

## Progress log

- 14:34 — Added SDK 56-compatible Expo image picker/file-system/crypto deps.
- 14:34 — Added idempotent `item-images` private bucket migration with owner-folder SELECT/INSERT/UPDATE/DELETE policies.
- 14:34 — Implemented direct client upload to versioned private paths and rollback deletion for failed PATCHes.
- 14:34 — Added `setItemImage()` API wrapper and mounted `ChangePhotoButton` on item detail.
- 14:34 — Hardened backend path validation tests for wrong item-id and query-string paths.
- 14:34 — Applied and verified the storage migration locally.

## Done

- `apps/backend/drizzle/0003_item_images_storage.sql` creates a private `item-images` bucket with 5 MB limit, JPEG/PNG/WebP allow-list, and four authenticated owner-folder policies.
- `apps/mobile/lib/media/uploadItemImage.ts` validates selected media, uploads an `ArrayBuffer` to `<userId>/<itemId>/<uuid>.<ext>`, and exposes rollback deletion.
- `apps/mobile/components/ChangePhotoButton.tsx` handles permission/validation/generic failures, rolls back uncommitted uploads, and upserts the returned DTO into the store.
- `apps/mobile/lib/api/items.ts` now exposes `setItemImage()`.
- `apps/mobile/app/(protected)/item/[id].tsx` renders the photo replacement control.
- `apps/mobile/app.config.ts` includes the iOS photo-library usage description.

## Left / next

- Continue with `integration/003` realtime item sync.
- Production Supabase storage migration was not applied because production mutation requires explicit approval. Local Supabase was migrated and verified.

## Gotchas

- Drizzle migration journal timestamps must be greater than the existing `0000` entry; `0003` uses `1781868000000` so Drizzle applies it after the original migration.
- `EXPO_PUBLIC_API_BASE_URL` includes `/api`; all item API wrappers continue to call paths like `/items/...`.
- The backend route already existed from backend/004; this phase hardened tests and added the storage migration/mobile UI.

## Files touched

- `apps/backend/drizzle/0003_item_images_storage.sql`
- `apps/backend/drizzle/meta/_journal.json`
- `apps/backend/db/migration.test.ts`
- `apps/backend/src/items/routes.test.ts`
- `apps/mobile/package.json`
- `pnpm-lock.yaml`
- `apps/mobile/app.config.ts`
- `apps/mobile/lib/media/uploadItemImage.ts`
- `apps/mobile/lib/api/items.ts`
- `apps/mobile/components/ChangePhotoButton.tsx`
- `apps/mobile/app/(protected)/item/[id].tsx`

## Verification

- pnpm gate: PASS   tests: PASS
- Local migration: PASS
- Local bucket/policies verification: PASS
- Production migration: PENDING explicit approval
