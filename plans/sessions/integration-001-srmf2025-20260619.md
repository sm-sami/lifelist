# Handoff — integration/001

- Author: srmf2025@gmail.com   Branch: phase/integration-001   Started: 2026-06-19 14:22Z   State: done

## Summary

Implemented the mobile data-layer plumbing: JWT-injecting API client, typed Zustand items store, hydration/reset hook, and dashboard-level hydration wiring.

## Progress log

- 14:22 — Built API client with Supabase token injection, timeout/cancellation, GET retry, and careful invalid-token 401 handling.
- 14:22 — Replaced item-store stubs with DTO-validating Zustand state, optimistic insert/upsert, deep-link fetch, and account-switch generation guard.
- 14:22 — Mounted item hydration in the protected tabs layout.
- 14:22 — Added focused Jest coverage for API auth behavior and item-store edge cases.

## Done

- `apps/mobile/lib/api/client.ts` now resolves paths against `EXPO_PUBLIC_API_BASE_URL`, injects the latest Supabase JWT, maps HTTP/network errors, and only signs out for explicit `token_invalid` / `token_expired` 401 responses.
- `apps/mobile/store/types.ts` aliases canonical shared DTOs instead of redefining fields.
- `apps/mobile/store/items.ts` provides `useItems`, `useItem`, and `useItemsStore` with hydrate/refetch/fetch/upsert/remove actions.
- `apps/mobile/hooks/useHydrateItems.ts` resets the store on login/logout/account switch and hydrates the active user.
- `apps/mobile/app/(protected)/(tabs)/_layout.tsx` calls `useHydrateItems()` so dashboard data loads on authenticated tab mount.
- Jest module aliasing was added for `@/*` mobile imports.

## Left / next

- Continue with `integration/002` media pipeline after this phase is merged to main.

## Gotchas

- `EXPO_PUBLIC_API_BASE_URL` examples include `/api`; the client intentionally appends only the path (`/items`, `/experiences`) to avoid double-prefixing.
- The generation guard in `hydrate()` is load-bearing for account-switch isolation.

## Files touched

- `apps/mobile/lib/api/client.ts`
- `apps/mobile/lib/api/client.test.ts`
- `apps/mobile/store/items.ts`
- `apps/mobile/store/items.test.ts`
- `apps/mobile/store/types.ts`
- `apps/mobile/hooks/useHydrateItems.ts`
- `apps/mobile/app/(protected)/(tabs)/_layout.tsx`
- `apps/mobile/package.json`

## Verification

- pnpm gate: PASS   tests: PASS
