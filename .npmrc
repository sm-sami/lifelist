# Use a flat install for predictable Expo/Metro native-module resolution.
node-linker=hoisted

# Keep workspace installs practical while preserving the lockfile as CI truth.
strict-peer-dependencies=false
auto-install-peers=true
